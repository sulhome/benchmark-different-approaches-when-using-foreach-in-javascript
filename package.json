{
  "name": "benchmark-different-approaches-when-using-foreach-in-javascript",
  "version": "1.0.0",
  "description": "compare the performance of running foreach in 5 different ways. Namely ES6, tail recursion (without a callback), tail recursion (with a callback), underscore and lodash",
  "main": "index.js",
  "scripts": {
    "watch": "tsc -w",
    "start": "tsc && node ./src/foreach.js 10"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sulhome/benchmark-different-approaches-when-using-foreach-in-javascript.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/sulhome/benchmark-different-approaches-when-using-foreach-in-javascript/issues"
  },
  "homepage": "https://github.com/sulhome/benchmark-different-approaches-when-using-foreach-in-javascript#readme",
  "dependencies": {
    "benchmark": "^2.1.3",
    "lodash": "^4.17.4",
    "randomstring": "^1.1.5",
    "underscore": "^1.8.3"
  },
  "devDependencies": {
    "@types/node": "^7.0.5",
    "typescript": "^2.2.1"
  }
}
